# week-01-assignment

I think I am now in a state where I am mostly happy my result!

- I struggled a lot trying to figure out how to include the position absolute, I tried adding after nearly everything was working and it just seemed to break things.
- I use darkmode a lot on my system and my default browser is Brave, I also have firefox and chromium installed with default configurations, so was able to see the text was all the wrong colour it was black, I fixed that by defining the colors in CSS and now the live site on git looks the same in both dark and lite mode browsers, I am aware that CSS has ways to handle this but did not investigate further yet.
- on reflection I should probably have set the text color on the body as its all the same for the whole page.
- The background images were tricky to configure the way I wanted but I got the result I was after in the end.
- On reflection I did not investigate the fonts as I was happy with the result as it stood.
- The home anchor was used as the back to top feature.
- I was going to remove the Rockets link as it has no real destination, but the dynamic handling for the nav bar is more pronounced with it in place so left it in.
- the About link was mapped to the footer to ensure it could be reached from the nav bar.
- Much of my research consisted of asking questions in the brave url bar - this defaults to an AI result, which nearly always gave examples, I do not copy and paste, and only use the examples to better understand the concepts.

Sources:

- The background images were downloaded from the example site and resized to a better resolution, keeping the same aspect ratio.
- The discord logo was downloaded from https://discord.com/branding and I used the svg rather than the image for smaller size benefit.
